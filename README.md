# std::enable\_shared\_from\_this example

## Building and running
```bash
cmake -H. -B_builds && make -C _builds && _builds/enable_shared_from_this
```

## References

- Effective Modern C++. Scott Meyers
